<% content_for :title, "Editar #{@pocket.name} - BankLinker" %>

<section class="space-y-6 max-w-xl">
  <div>
    <h1 class="text-3xl font-bold">Editar pocket</h1>
    <p class="text-slate-600 mt-1">Ajusta el nombre, el color o la posición.</p>
  </div>

  <div class="bg-white rounded-xl border border-slate-200 p-6">
    <%= render "form", pocket: @pocket %>
  </div>
</section>
